# Local test subjects — full clones for testing, never part of this repo
test-subjects/

# PowerShell / dotnet test artifacts
*.log
TestResults/
testResults.xml
.vs/
bin/
obj/

# Claude Code runtime lock (per-session; not for version control)
.claude/scheduled_tasks.lock
# Per-user local Claude Code settings (machine-local; never committed)
.claude/settings.local.json

# Generated UX transcript (machine-specific temp paths; regenerate with Show-CommandUx.ps1)
tools/ux-flow.md
